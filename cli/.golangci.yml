version: "2"

linters:
  default: standard
  enable:
    - errorlint
    - bodyclose
    - noctx
    - copyloopvar
    - misspell
    - unparam
    - gocritic
    - revive
  settings:
    errcheck:
      check-blank: true
    misspell:
      ignore-rules:
        - comando
    revive:
      rules:
        - name: package-comments
          disabled: true
        - name: exported
          disabled: true

formatters:
  enable:
    - gofmt
    - goimports
